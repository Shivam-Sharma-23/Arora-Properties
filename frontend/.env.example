# Base URL of the backend (separate repo, see CLAUDE.md). Not needed locally
# unless you want to point at something other than the default
# (http://localhost:4000, i.e. the backend running via `npm run dev` there).
# On Netlify, set this to the deployed Render URL:
VITE_API_URL=https://arora-backend-nogt.onrender.com
